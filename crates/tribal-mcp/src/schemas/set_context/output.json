{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "McpSetContextResponse",
  "description": "Typed response for `tribal_set_context`.\n\nThe `project` field is non-required in the output schema but always included (null when absent) for consistency with the session resource representation.",
  "type": "object",
  "required": [
    "actor",
    "principal_key"
  ],
  "properties": {
    "actor": {
      "$ref": "#/definitions/McpSessionActor"
    },
    "principal_key": {
      "type": "string"
    },
    "project": {
      "anyOf": [
        {
          "$ref": "#/definitions/McpSessionProject"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "definitions": {
    "GitRemote": {
      "description": "Protocol-agnostic git remote identity.\n\nStores the canonical `host/path` form (e.g. `github.com/user/repo`), stripping the transport scheme and `.git` suffix. Two remotes that point to the same repository — regardless of whether they use SSH, HTTPS, or SCP syntax — produce the same `GitRemote` value.\n\nNon-standard ports are preserved in the canonical form as `host:port/path` since different ports may serve different content.\n\nAll major git hosts (GitHub, GitLab, Bitbucket, Gitea) treat organisation and repository names as case-insensitive, so the canonical form is fully lowercased.\n\n# Construction\n\nParse any common git remote URL format via [`FromStr`]:\n\n```text git@github.com:user/repo.git   → github.com/user/repo https://github.com/user/repo   → github.com/user/repo ssh://git@github.com/user/repo → github.com/user/repo ```\n\nOr build from already-parsed components via [`GitRemote::from_parts`].",
      "type": "object",
      "required": [
        "canonical"
      ],
      "properties": {
        "canonical": {
          "type": "string"
        }
      }
    },
    "McpSessionActor": {
      "description": "Actor metadata on the MCP `set_context` response surface.\n\nAll fields are always present in serialised JSON (null when absent), matching the existing session resource representation.",
      "type": "object",
      "properties": {
        "client_name": {
          "type": [
            "string",
            "null"
          ]
        },
        "client_version": {
          "type": [
            "string",
            "null"
          ]
        },
        "model": {
          "type": [
            "string",
            "null"
          ]
        },
        "provider": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "McpSessionProject": {
      "description": "Project on the MCP `set_context` response surface.",
      "type": "object",
      "required": [
        "id",
        "name",
        "origin"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "origin": {
          "$ref": "#/definitions/ProjectOrigin"
        }
      }
    },
    "ProjectOrigin": {
      "description": "The source of a project's stable identity.",
      "oneOf": [
        {
          "description": "The graph-wide project maintained by Tribal.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "system"
              ]
            }
          }
        },
        {
          "description": "A project established from a Git remote.",
          "type": "object",
          "required": [
            "default_branch",
            "kind",
            "remote"
          ],
          "properties": {
            "default_branch": {
              "description": "Branch used when a caller supplies no branch.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "enum": [
                "git"
              ]
            },
            "remote": {
              "description": "Canonical remote identity.",
              "allOf": [
                {
                  "$ref": "#/definitions/GitRemote"
                }
              ]
            }
          }
        }
      ]
    }
  }
}
