{
  "type": "object",
  "description": "Start a reindex to a new embedding geometry. The target is named on the command, never read from config. Reads and writes continue against the active profile while the new space fills; the swap is atomic. Operator-only.",
  "properties": {
    "provider": {
      "type": "string",
      "description": "The target embedding provider, for example 'ollama' or 'openai'."
    },
    "model": {
      "type": "string",
      "description": "The target embedding model."
    },
    "dimensions": {
      "type": "integer",
      "description": "The target vector dimension. Resolved from the model when omitted."
    },
    "base_url": {
      "type": "string",
      "description": "The target endpoint. The provider's default when omitted."
    }
  },
  "required": ["provider", "model"],
  "additionalProperties": false
}
