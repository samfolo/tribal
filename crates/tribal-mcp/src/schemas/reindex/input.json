{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "McpReindexRequest",
  "description": "The MCP request for `tribal_reindex`.",
  "type": "object",
  "required": [
    "model",
    "provider"
  ],
  "properties": {
    "base_url": {
      "description": "The target endpoint; the provider's default when omitted.",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "dimensions": {
      "description": "The target vector dimension; resolved from the model when omitted.",
      "default": null,
      "type": [
        "integer",
        "null"
      ],
      "format": "uint32",
      "minimum": 0.0
    },
    "dry_run": {
      "description": "When true, resolve and estimate the target without creating a run.",
      "default": false,
      "type": "boolean"
    },
    "model": {
      "description": "The target embedding model.",
      "type": "string"
    },
    "provider": {
      "description": "The target embedding provider (for example `\"ollama\"` or `\"openai\"`).",
      "type": "string"
    }
  }
}
