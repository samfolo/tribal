× config_validate               
    server.bind_address cannot be set when server.transport is stdio
    embedding.api_key is required when embedding.provider is openai
    inference.triage.api_key is required when inference.triage.provider is
    openai
    fix: server.bind_address cannot be set when server.transport is stdio
    set `embedding.api_key` or export `TRIBAL_EMBEDDING__API_KEY`
    set `inference.triage.api_key` or export `TRIBAL_INFERENCE__TRIAGE__API_KEY`